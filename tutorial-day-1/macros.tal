( macros.tal )

( devices )
|10 @Console
	[ &vector $2 &read $1 &pad $5 &write $1 &err $1 ]

( macros )
%EMIT { .Console/write DEO } ( character -- )
%NL { #0a EMIT } ( -- )

( main )
|0100
LIT "h EMIT
NL
